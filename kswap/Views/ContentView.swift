import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack (spacing: 0){
            TopBar()
            NavBar()
        }
        
        
        
        
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
